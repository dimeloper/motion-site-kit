# Examples

Three demos, each a live link. A demo someone can scroll on their phone right
now does the work that a description cannot.

| Demo | Vertical | Live |
|---|---|---|
| `saas/` | SaaS landing page | _pending_ |
| `commerce/` | Single-product e-commerce | _pending_ |
| `local/` | Local business | _pending_ |

Each is a copy of `template/` with its own `config.js` and frame ladder —
which is the point. If building the second one takes as long as the first,
something has leaked out of `config.js` and into `src/motion.js`.
