# Examples

| Demo | Brand | Motion | Status |
|---|---|---|---|
| `local/` | Harbor Oven | Live WebGL loaf orbit + flour motes | Craft reference — reuse patterns for Vortex |
| `saas/` | **Vortex** | CAD ring: peel → seat | Active — solid torus, inner sensors |
| `commerce/` | **Halo** studio | Rings lift and seat | Composed from `docs/kit/`. Stone + light rings. |

Serve: `python3 -m http.server 8080 --directory docs` → `/kit/`, `/examples/local/`, `/examples/saas/`, `/examples/commerce/`.

Default kit path remains `template/` (frame scrub). Section families: `docs/kit/`. WebGL craft notes: `skills/motion-website/references/webgl-model.md`.
