# docs/ — GitHub Pages / Netlify publish dir

This directory is a copy of `template/` with its own `config.js`. The copy is
the README: you are scrolling the verification clip, inside the budget, on the
same engine every project uses.

`src/motion.js` is identical to `template/src/motion.js`. If it diverges, push
the change back into the template or into config.

Frames here are committed so the published site works without ffmpeg on the host.
Regenerate them with the pipeline in the root README if the source clip changes.
