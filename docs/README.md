# docs/ — GitHub Pages

This directory is the published documentation site, and it is built as a
scroll-driven motion site using the kit itself. The documentation is the demo;
a text article about motion cannot make that argument.

Not built yet. To build it, run the skill against this repo's own README and
reference docs as the content source.
