# Examples

| Demo | Brand | Motion | Status |
|---|---|---|---|
| `local/` | Harbor Oven | Live WebGL loaf orbit + flour motes | Craft reference — reuse patterns for Vortex |
| `saas/` | **Vortex** | Explode → assemble console | Stub — build next from Harbor checklist |
| `commerce/` | **Halo** | Material peel | Stub — after Vortex |

Serve: `python3 -m http.server 8080 --directory docs` → `/examples/local/`, `/examples/saas/`, `/examples/commerce/`.

Default kit path remains `template/` (frame scrub). Live WebGL craft notes (Harbor → Vortex): `skills/motion-website/references/webgl-model.md`.
